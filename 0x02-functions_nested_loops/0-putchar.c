#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Write a program that prints _putchar,
 * followed by a new line.
 *
 * Return Always 0 (success)
 */
int main(void)
{
	char ch = 's';

	_putchar(ch);
	_putchar('\n');

	return (0);
}
